import styles from './App.module.css';

export default function App() {
  return (
    <main className={styles.container}>
      <h1 className={styles.title}>Hello PetCircle</h1>
      <p className={styles.subtitle}>React + Vite + TypeScript + CSS Modules</p>
    </main>
  );
}
